{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Generate Protocol Code",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "tsx",
      "runtimeArgs": ["--no-warnings"],

      "args": ["scripts/generate-protocol-code.ts"],

      "cwd": "${workspaceRoot}",
      "internalConsoleOptions": "openOnSessionStart",
      "skipFiles": ["<node_internals>/**", "node_modules/**"]
    }
  ]
}
